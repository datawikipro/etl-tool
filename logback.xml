<configuration>
    <!-- Suppress logback status messages -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener" />
    
    <!-- Конфигурация appender для вывода в консоль -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Root level WARN to capture any library warnings/errors, but keep it clean -->
    <root level="WARN">
        <appender-ref ref="STDOUT"/>
    </root>

    <!-- Application code: log progress at INFO level -->
    <logger name="pro.datawiki" level="INFO"/>

    <!-- Quiet down third-party libraries (WARN or ERROR) -->
    <logger name="com.amazonaws" level="WARN"/>
    <logger name="org.apache" level="WARN"/>
    <logger name="org.apache.spark" level="WARN"/>
    <logger name="org.apache.hadoop" level="WARN"/>
    <logger name="httpclient" level="WARN"/>
    <logger name="com.clickhouse" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
    <logger name="org.eclipse.jetty" level="WARN"/>
    <logger name="akka" level="WARN"/>
    <logger name="org.mongodb" level="WARN"/>
    <logger name="com.mongodb" level="WARN"/>
</configuration>