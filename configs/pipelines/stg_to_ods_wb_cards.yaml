# Pipeline: STG -> ODS (Avro -> Iceberg)
# Source: s3://bi-dev/warehouse/stg__wb.db/cards/ (Avro)
# Target: s3://bi-dev/warehouse/ods__wb.db/cards/ (Iceberg, Hive Metastore)

connections:
  - sourceName: "stg_avro"
    connection: "minioAvro"
    configLocation: "configs/connections/local_stg_avro.yaml"
  - sourceName: "ods_iceberg"
    connection: "minioIceberg"
    configLocation: "configs/connections/yandex_s3_iceberg_ods_hadoop.yaml"

source:
  - sourceName: "stg_avro"
    objectName: "stg_cards"
    initMode: "instantly"
    sourceFileSystem:
      tableName: "warehouse/stg__wb.db/cards"
      partitionBy:
        - "partition"
      where: "partition = '${partition}'"

target:
  - fileSystem:
      connection: "ods_iceberg"
      source: "stg_cards"
      tableName: "ods__wb.cards"
      targetFile: "ods__wb.cards"
      mode: "overwriteTable"
