package pro.datawiki.sparkLoader.connection.postgres

case class YamlServerHost(
                           host: String,
                           port: String,
                           database: String
                         )
