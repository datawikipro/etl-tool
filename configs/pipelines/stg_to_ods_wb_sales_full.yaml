# Pipeline: STG -> ODS (Avro -> Iceberg)
# Source: s3://bi-dev/warehouse/stg__wb.db/sales/ (Avro)
# Target: s3://bi-dev/warehouse/ods__wb/sales/ (Iceberg, Hive Metastore)

connections:
  - sourceName: "stg_avro"
    connection: "minioAvro"
    configLocation: "configs/connections/local_stg_avro.yaml"
  - sourceName: "ods_iceberg"
    connection: "minioIceberg"
    configLocation: "configs/connections/yandex_s3_iceberg_ods_hadoop.yaml"

source:
  - sourceName: "stg_avro"
    objectName: "stg_sales"
    initMode: "instantly"
    sourceFileSystem:
      tableName: "warehouse/stg__wb.db/sales"
      partitionBy:
        - "partition"

target:
  - fileSystem:
      connection: "ods_iceberg"
      source: "stg_sales"
      tableName: "ods__wb.sales"
      targetFile: "ods__wb.sales"
      mode: "overwriteTable"
