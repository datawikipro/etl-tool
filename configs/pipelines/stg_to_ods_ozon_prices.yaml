# Pipeline: STG -> ODS (Avro -> Iceberg)
# Source: s3://bi-dev/warehouse/stg__ozon.db/prices/ (Avro)
# Target: s3://bi-dev/warehouse/ods__ozon.db/prices/ (Iceberg, Hive Metastore)

connections:
  - sourceName: "stg_avro"
    connection: "minioAvro"
    configLocation: "configs/connections/local_stg_avro.yaml"
  - sourceName: "ods_iceberg"
    connection: "minioIceberg"
    configLocation: "configs/connections/yandex_s3_iceberg_ods_hadoop.yaml"

source:
  - sourceName: "stg_avro"
    objectName: "stg_prices"
    initMode: "instantly"
    sourceFileSystem:
      tableName: "warehouse/stg__ozon.db/prices"
      partitionBy:
        - "partition"
      where: "partition = '${partition}'"

target:
  - fileSystem:
      connection: "ods_iceberg"
      source: "stg_prices"
      tableName: "ods__ozon.prices"
      targetFile: "ods__ozon.prices"
      mode: "overwriteTable"
